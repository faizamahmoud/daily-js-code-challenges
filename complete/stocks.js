// brute force


function bruteForce(arr) {
    for (let i = 0; i < arr.length; i++) {
        for (let j = i+1; j < arr.length; j++) {
            // check if arr[i] and arr[j] meet the conditions
        }
    }
}
